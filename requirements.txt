# --- One-shot-ish install ---
# Prefer (CHTC GPU): pip install -r requirements-chtc.txt  then  pip install -r requirements.txt
# so PyTorch stays on the cu118 wheels. Pure CPU/laptop can use only this file.
#
# Heavy / optional deps used by other OTDD modules (datasets, flows, demos) live in:
# ORCA/src/otdd/requirements.txt — add manually if you need those code paths.
#

# Core dependencies (CHTC: install torch cu118 from requirements-chtc.txt first)
torch>=2.0.0
transformers>=4.30.0
peft>=0.4.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0
munch>=4.0.0
PyYAML>=6.0
Pillow>=9.0.0

# OT / optimal transport stack (embedder → otdd.pytorch.distance and helpers)
geomloss>=0.2.6
POT>=0.9.0
tqdm>=4.65.0
munkres>=1.1.4
joblib>=1.2.0

# Shared vision / NLP utilities (already common with torch transformers)
einops>=0.6.0
timm>=0.9.0
gdown>=4.7.0

# Data loading & misc
h5py>=3.8.0
pandas>=2.0.0

# Plotting / optional OTDD tooling (matplotlib always needed by otdd at import); seaborn/adjustText
# only for specific otdd plotting helpers — safe to omit on minimal CHTC if you lazy-import stays in sync.
matplotlib>=3.7.0
